# 2026-08-01 工作日志

## 生产设备预测性维护与智能运维平台开发
- 项目路径: `/Users/cayden/WorkBuddy/2026-07-31-17-28-37/predictive_maintenance/`
- 技术栈: Flask + Vanilla JS + ECharts + D3.js + SQLite
- 端口: 5100
- Python venv: `/Users/cayden/.workbuddy/binaries/python/envs/medical/`（复用医学影像项目的venv）

### 完成功能
1. **传感器数据模拟引擎** - 模拟振动/温度/电流/压力/转速/流量/声学7类传感器，支持正常→退化→故障渐进式演变
2. **AI异常检测引擎** - 统计阈值检测 + Z-score异常检测 + 趋势分析 + 健康度评分(0-100) + RUL剩余寿命预测
3. **故障诊断引擎** - 8种故障模式知识库(轴承磨损/转子不平衡/轴系不对中/过载/润滑失效/气蚀/密封失效/电气故障)，基于征兆-权重矩阵推理
4. **数据库与运维管理** - SQLite存储设备/传感器数据/告警/工单/健康历史，支持告警确认、工单生命周期管理
5. **故障知识图谱** - 105节点/156边的力导向图(D3.js)，包含设备-故障-征兆-原因-处置方案-维护策略关系网络
6. **完整前端界面** - 总览看板(KPI+图表)、设备监控(5台默认设备)、设备详情(传感器曲线+健康度趋势+AI诊断)、告警中心、维修工单、故障诊断、知识图谱可视化、系统设置

### 架构决策
- **按需数据采集模式**：放弃后台线程方案（Flask dev server threading与后台线程存在锁争用导致POST请求超时），改为由API请求触发tick_data()按需生成数据，支持补齐丢失周期（单次最多10个周期）
- **SQLite WAL + busy_timeout**：解决并发读写锁争用
- **非debug模式**：避免Flask reloader干扰

### 关键修复
- knowledge_graph.py: 3元素元组解包错误(改为3变量解包)
- database.py: 添加busy_timeout=5000解决SQLite锁争用
- app.py: 后台线程→按需数据采集模式，提取_run_one_data_cycle()独立函数
- anomaly_detector.py: predict_rul方法health_trend字典列表类型适配
- app.py: init_equipment始终初始化模拟器状态(之前设备已存在时跳过)

### 后续修复与补充
- 修复知识图谱前端不显示：API返回缺少success字段 + 节点详情API嵌套层级问题
- 输出真实传感器数据对接方案(INTEGRATION_GUIDE.md)：含时序图、数据适配器代码(MQTT+HTTP双模式)、配置扩展、部署方案、边缘网关采集脚本示例
- 两个项目已提交到GitHub: https://github.com/cayden/ai-diagnostic-platforms
- 输出predictive_maintenance README.md（快速开始、配置说明、API文档、故障知识库、演示流程、AI算法说明、对接指南引用）

## AI质检视觉检测平台开发
- 项目路径: `/Users/cayden/WorkBuddy/2026-07-31-17-28-37/ai_quality_inspection/`
- 技术栈: Flask + Vanilla JS + ECharts + Canvas + SQLite
- 端口: 5101
- Python venv: 复用 `/Users/cayden/.workbuddy/binaries/python/envs/medical/`

### 完成功能
1. **YOLO缺陷检测引擎** - 10类缺陷(划痕/凹痕/裂纹/污渍/变形/缺件/错装/色差/毛刺/氧化)，模拟模式+真实模型双模式
2. **Deepseek LLM分析** - 根因分析、处置建议(接收/返工/报废/评审)、风险评估、改进建议、后续行动
3. **质量判定** - 三级缺陷严重度(critical/major/minor)综合判定合格/不合格
4. **质量追溯** - SQLite存储检测记录，支持按产品/批次/记录ID搜索，批次统计
5. **SPC统计过程控制** - 不良率控制图(UCL/CL/LCL)、每日趋势、缺陷类型分布、产线对比
6. **缺陷知识图谱** - 48节点/146边(缺陷-成因-工序-处置-预防-检测方法关系网络)，力导向图可视化
7. **AI问答助手** - 内置知识库覆盖10类缺陷分析、SPC、判定标准、检测原理、追溯、批量检测
8. **批量检测** - 支持多图上传批量检测，自动汇总批次合格率
9. **完整前端** - 质检看板(5指标卡+4图表+批次排行)、影像检测(上传+Canvas标注框+LLM分析)、检测记录、质量统计、AI助手、知识图谱、系统设置

### 架构决策
- 复用medical项目venv（flask/Pillow/requests已安装）
- 模拟检测模式基于ImageStat亮度/对比度分析+随机种子生成检测结果
- 知识图谱API直接返回success字段避免前端不渲染问题（吸取预测性维护项目的教训）
